export default function Search() {
  return (
    <div>
      <h1 className="text-center text-2xl font-bold">
        News About: <span>{
            const {state} = useLocation();
            console.log(state);
            }</span>
      </h1>
    </div>
  );
}
